# 决策：导航条右缘距收窄（12px → 3px）与发布 0.1.2

日期：2026-08-19
状态：已完成

## 一、问题

用户反馈：.crl_nav 的 `right: calc(var(--dsh-sidebar-width, 0px) + 12px)` 遮挡会话面板内容。

## 二、处理

1. src/client/index.tsx：`.crl_nav` right 偏移 12px → 3px。
2. 验证：pnpm test 9/9、typecheck、构建产物 grep 确认。
3. 提交 `cdd4b1a` 推送 main。
4. web/ssid 两个 profile 切 link 本地即时生效，用户确认效果满意。
5. 发布 **0.1.2**（npm + GitHub tag v0.1.2），两个 profile 保持 link 本地。
